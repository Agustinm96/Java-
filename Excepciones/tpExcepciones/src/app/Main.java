package app;

import java.util.Scanner;

public class Main {
    static Scanner teclado;

    public static void main(String[] args) {
        /*teclado = new Scanner(System.in);

        Juego adivinar = new Juego();
        boolean rta = false;

        while (!rta)
        {
            try {
                rta = adivinar.adivinar(teclado.nextInt());
            }
            catch (NumeroIncorrecto e)
            {
                System.out.println(e.getMessage());
                adivinar.incCantIntentos();
            }
            catch (Exception e)
            {
                teclado.nextLine();
                System.out.println(e.getMessage());
                adivinar.incCantIntentos();
            }
            finally {
                if (rta)
                {
                    System.out.println("FELICIDADES.\nNUMERO DE INTENTOS: "+adivinar.getCantIntentos());
                }
            }
        }*/



    }
}
